# 系统策划自定义场景索引

| 场景ID | 名称 | 触发关键词 | 描述 |
|--------|------|-----------|------|
| `{scene_id}` | {场景名称} | {关键词1}、{关键词2} | {场景用途} |

> 在此目录下放置系统策划专属的自定义场景文件。
> 文件命名建议与场景ID一致，如 `feedback_system.md`。
> 示例见同目录下的 `example.md`。
